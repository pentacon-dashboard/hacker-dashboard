/**
 * openapi-typescript 를 사용해 shared/openapi.json -> shared/types/api.ts 를 생성한다.
 * package.json 의 "gen:api" 스크립트로 실행된다.
 *
 * 실행: npm run gen:api
 * 의존: openapi-typescript (devDependency)
 */
// 실제 생성은 package.json 스크립트의 CLI 호출로 처리.
// 이 파일은 gen:api 커맨드의 설명 문서 역할을 한다.
export {};
