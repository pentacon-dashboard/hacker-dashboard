# 목업 8페이지 vs 현재 구현 — 정밀 Gap 분석

작성: 2026-04-23
출처: `/c/Users/ehgus/Downloads/KakaoTalk_20260423_185428043[_01..07].png` (목업 8장)
비교 방법: Playwright MCP 로컬 dev(`http://localhost:3000`, 1440×900, `.dark`) 풀페이지 캡처 후 1:1 육안 대조
캡처 결과: `.playwright-mcp/current-{페이지}.png` 저장 (8장)

> **중요 전제**: 현재 BE가 실데이터 2종목(KRW-BTC + AAPL) 기반이라 **수치는 목업과 다르지만 가능**. 이 리포트는 **레이아웃·컴포넌트·라우트 구조**의 gap 에만 집중한다.

---

## 0. 글로벌 Gap — 사이드바 IA(Information Architecture)

| 구분 | 목업 (8개) | 현재 (6개) |
|---|---|---|
| 1 | 대시보드 | 대시보드 ✅ |
| 2 | 포트폴리오 | 포트폴리오 ✅ |
| 3 | 워치리스트 | 워치리스트 ✅ |
| 4 | **종목 분석** | (심볼 페이지는 있으나 사이드바 엔트리 없음) |
| 5 | **시장 분석** | **분석** (준비중 스텁) ← 기능 부재 |
| 6 | **코파일럿** | (헤더 검색/Drawer 만, 전용 페이지 없음) |
| 7 | **업로드 & 분석** | (라우트 404) |
| 8 | 설정 | **설정** (준비중 스텁) |
| — | — | 뉴스 (목업에 없음) |

**결론**:
- 현재 사이드바는 "뉴스" 가 1개 있고, 목업의 "종목 분석 / 시장 분석 / 코파일럿 / 업로드&분석" 4개가 **빠져있다**.
- 심사 임팩트가 가장 큰 것은 **06 업로드&분석** (CLAUDE.md의 "임의 CSV → 5초 내 자동 대시보드" = 심사 기준 #2 데모 임팩트의 핵심).

**변경 방안 A** — 사이드바 IA 재편 (파일: `frontend/components/layout/sidebar.tsx`, `app/layout.tsx`)
```
대시보드 / 포트폴리오 / 워치리스트 / 종목 분석 / 시장 분석 / 코파일럿 / 업로드 & 분석 / 설정
```
- 종목 분석은 `watchlist`/`portfolio`에서 클릭 진입하던 기존 동선 유지 + 사이드바에 "최근 조회" 목록으로 노출
- 뉴스는 대시보드 홈의 사이드 패널로 흡수(이미 존재). 사이드바 엔트리 제거.
- `/upload`, `/copilot`, `/market-analyze` 라우트 신규

---

## 1. 대시보드 홈 (`/`) — 목업 `00`

| 영역 | 목업 | 현재 | Gap |
|---|---|---|---|
| 헤더 | 날짜 레인지 picker + CSV 업로드 버튼 + 알림/다크 토글 | Copilot 검색바 + API 정상 + 다크 토글 | 데이트레인지·CSV 업로드 버튼 부재 |
| KPI | 6개(총자산/일간변동/일손익/30일/보유종목/집중도) | 6개 있음 ✅ | **KPI 1~2번째 값 truncate** ("₩115..." "+218...") 재발, formatKRWCompact 값 폭 초과 |
| 좌측 중단 | **자산 가치 추이** 라인차트 큰 사이즈 (1열) | `포트폴리오 비중` 도넛 (1열) | **좌-우 배치 순서 반대**. 목업은 왼쪽이 큰 라인차트, 오른쪽이 도넛 |
| 우측 중단 | **자산 배분 (도넛 + 라벨/금액/비중 3컬럼 리스트)** | 큰 라인차트 (자산 가치 추이) | 〃 위와 같은 이슈 |
| 최우측 | **집중도 리스크 게이지** (rose 68.3%) | 집중도 리스크 게이지 (rose 100%) | 수치는 BE 의존 / 디자인은 매치 ✅ |
| 하단좌 | 보유 자산 TOP 5 (6컬럼) | 보유 자산 TOP 5 (6컬럼) ✅ | OK |
| 하단중 | 디멘션 분석 (2시리즈 바) | 디멘션 분석 ✅ | OK |
| 하단우 | **시장 주도주** (3종목 카드: 회사/티커/가격/변동 + 썸네일) | "관련 뉴스 없음" 문구 | 컴포넌트 자체 다름: 목업 = 종목 카드 3개(리더보드), 현재 = 뉴스 패널 |

**변경 방안 B**
1. `app/page.tsx` 의 **중단 2열 교체**: 왼쪽=자산가치 추이(라인), 오른쪽=AllocationBreakdown(도넛+리스트). 현재는 반대.
2. KPI truncate 재발 — `KpiCard` max-width 또는 `formatKRWCompact`가 fullPage 1440에도 잘린다. `text-2xl md:text-[1.375rem]` 로 다운 + 자릿수 7자 초과 시 `M` 단위로 축약 강제.
3. 하단 우측 뉴스 영역을 **"시장 주도주 Top 3" 카드**로 교체(신규 `<MarketLeaders>` 컴포넌트). 현재 뉴스 피드는 코파일럿 drawer/sidebar 에서만 노출.
4. 헤더에 **데이트레인지 picker**(shadcn DateRangePicker) + **CSV 업로드 버튼** 신설. CSV 업로드는 `/upload` 로 라우팅.

---

## 2. 포트폴리오 (`/portfolio`) — 목업 `01`

| 영역 | 목업 | 현재 | Gap |
|---|---|---|---|
| KPI | 5개(총자산/평가손익/일간변동/보유종목/승률 68% 게이지) | 3개(총자산/평가손익/일간변동) | **KPI 2개 부재**: 보유종목 카운트, 승률 게이지 |
| 상단 중앙 | 보유자산 리스트 "자산 한눈에" 10행 테이블 + 필터 탭 + 정렬 | 자산군 비중 도넛 + 순자산 추이 라인 | **핵심 테이블이 좌측 아래로 밀림**, 상단 구성 다름 |
| 우측 | 자산 구성 도넛 + 평가금액 리스트 | (없음) | 상단 우측 `자산 구성` 카드 부재 |
| 좌하 | **섹터 히트맵** (섹터별 수익률 색상) | (없음) | 완전 부재 |
| 중하 | **월간 수익률 히트맵** (365칸 달력) | (없음) | 완전 부재 |
| 우하 | **AI 인사이트 요약** (stub 배지 + 3문장) | 종목별 손익 테이블 | 컴포넌트 자체 다름 |

**변경 방안 C** (신규 컴포넌트 3개)
- `frontend/components/portfolio/sector-heatmap.tsx` — 섹터별 가중수익률 타일(5×3 그리드)
- `frontend/components/portfolio/monthly-return-calendar.tsx` — GitHub contribution 스타일 365칸 달력
- `frontend/components/portfolio/ai-insight-card.tsx` — ADR 0012 stub 모드 기반 3줄 요약 카드
- KPI를 5개로 확장: `holdings_count`(이미 BE 존재), `win_rate_pct` 필드 추가 필요(BE 스키마 업데이트)

---

## 3. 워치리스트 (`/watchlist`) — 목업 `02`

| 영역 | 목업 | 현재 | Gap |
|---|---|---|---|
| KPI | 4개(관심종목 수/상승평균/하락평균/최고 수익률) | (없음) | **완전 부재** |
| 좌 | 워치리스트 전체 테이블 (9컬럼: 심볼/현재가/변동/7일 차트/거래량/시가총액/수익률) | 빈 empty state | **테이블 자체 부재** (empty only) |
| 우 | 인기 TOP 5 + 등락 TOP 5 사이드 리스트 | (없음) | 완전 부재 |
| 하단 | 알림 설정 카드 + 맞춤 알림 설정 + 최근 체결 | (없음) | 완전 부재 |

**변경 방안 D** — 워치리스트는 **현재 기능이 가장 얇음**. MSW 픽스처로라도 8~10종목 시드를 박고 테이블·사이드 패널을 구현해야 시연 가능.
- `app/watchlist/page.tsx` 재작성
- `frontend/components/watchlist/watchlist-table.tsx` — 9컬럼, 7일 스파크라인 셀
- `frontend/components/watchlist/popular-top5.tsx`, `gainers-losers-top5.tsx`
- MSW `tests/mocks/watchlist.ts` 신규

---

## 4. 종목 분석 (`/symbol/[market]/[code]`) — 목업 `03`

| 영역 | 목업 | 현재 | Gap |
|---|---|---|---|
| 상단 | 티커 + 가격 + 변동 + 타임프레임 탭(1/5/15/60분/일/주/월) | 티커 + 가격 + 변동 (탭 없음) | 타임프레임 탭 부재 |
| 메인 차트 | 캔들 차트 + MA20/MA60 ✅ | ✅ | OK |
| 보조 차트 | RSI + MACD 2개 패널 | (없음) | 부재 |
| 우측 | 기본 정보 + 기술 지표 패널(RSI/MACD/볼린저/스토캐스틱 수치 리스트) + 주요 이슈 리스트 | 기본 정보 + Router 분석 근거 + AI 분석 (로딩중) | 기술 지표 리스트 부재, 주요 이슈 리스트 부재 |
| 하단 | 지표 카드 6개(등락률/평단/MA/거래량/수익률/시그널) + 관련 뉴스 패널 | AI 분석 섹션(로딩중) | 카드·뉴스 부재 |

**변경 방안 E**
- TradingView Lightweight Charts 는 이미 있으므로 RSI/MACD 패널 서브 차트 2개 추가 (`addLineSeries`)
- `frontend/components/symbol/indicator-grid.tsx` — 6지표 카드
- `frontend/components/symbol/news-panel.tsx` — 심볼별 뉴스 (홈의 NewsPanel 재사용 + prop `symbol`)

---

## 5. 시장 분석 (`/analyze`) — 목업 `04`

| 영역 | 목업 | 현재 | Gap |
|---|---|---|---|
| 전체 | 지수 KPI 5개(S&P/나스닥/다우/VIX/USD) + 글로벌 히트맵 세계지도 + 섹터 KPI 3개(하락/횡보/상승) + 주요 인덱스 리스트 + 섹터 KPI + 코모디티 + 주요 뉴스 | **"준비 중입니다" 스텁 한 줄** | **완전 부재** |

**변경 방안 F** — 가장 큰 공백. 심사 스코프에 포함시킬지 **우선순위 결정 필요**.
- 옵션 F-1(최소): 지수 KPI 5개 + 섹터 KPI 3개 + 주요 뉴스 3단만 구현, 세계지도/코모디티는 시연 생략
- 옵션 F-2(전체): 세계지도 heatmap(`react-simple-maps` 같은 라이브러리)까지 — 구현 비용 높음
- **권장: F-1**. MVP 범위로는 KPI 스트립 + 주요 뉴스가 가장 보여줄 수 있는 것. 데이터는 stub JSON.

---

## 6. 코파일럿 (`/copilot`) — 목업 `05`

| 영역 | 목업 | 현재 | Gap |
|---|---|---|---|
| 전용 라우트 | **풀페이지** `/copilot` (좌: 세션 목록 + 중: 대화 스레드 + 우: 참고 자료 패널) | **404 — 라우트 없음**. 헤더 검색창/Drawer 로만 접근 | 풀페이지 부재 |
| 세션 히스토리 | 좌측 사이드 리스트 | (없음) | 부재 |
| 메시지 스레드 | 유저 질문 + plan 카드 + step 카드들 + 최종 답변 | Drawer 내 단순 스트림 | 레이아웃/히스토리 부재 |
| 우측 | 포트폴리오 요약 + AI 인사이트 요약 + 최근 활동 | (없음) | 부재 |

**변경 방안 G** — `app/copilot/page.tsx` 신규. 기존 `useCopilotSession` 훅/스토어 그대로 재사용 가능. Drawer 로직을 풀페이지 레이아웃으로 재배치만. 세션 히스토리는 `/copilot/[session_id]` 로 라우트.
- 데모 시나리오 (기존 demo-rehearsal 문서 참조) 를 풀페이지에서도 동일하게 재현

---

## 7. 업로드 & 분석 (`/upload`) — 목업 `06`

| 영역 | 목업 | 현재 | Gap |
|---|---|---|---|
| 라우트 | 풀페이지 `/upload` | **404** | **완전 부재** |
| 1번 카드 | CSV 업로드 드래그 영역 + 선택된 파일 진행도 | (없음) | 부재 |
| 2번 카드 | 데이터 검증 (125행/123행 OK/2 오류/0 경고) | (없음) | 부재 |
| 3번 카드 | CSV 미리보기 테이블 5행 | (없음) | 부재 |
| 4번 카드 | 분석 설정 (Analyzer 선택 / 기간 / 통화) | (없음) | 부재 |
| 5번 카드 | 분석 진행 상태 (Router/Analyzer 단계별 배지) | (없음) | 부재 |
| 하단 | CSV 템플릿 다운로드 + 자주 묻는 질문 + 변환 옵션 | (없음) | 부재 |

**변경 방안 H** — **심사 기준 #2(데모 임팩트) 직결**. 최우선 구현.
- `app/upload/page.tsx` 신규 + `components/upload/*.tsx` 5개 카드
- 업로드 → `POST /upload/csv` → 검증 결과 stream (`schema gate → domain gate → critique gate` 3단 배지 순차 활성화)
- BE 엔드포인트: 이미 `backend/app/api/analyze.py` 비슷한 게 있다면 연결, 없으면 신규

---

## 8. 설정 (`/settings`) — 목업 `07`

| 영역 | 목업 | 현재 | Gap |
|---|---|---|---|
| 전체 | 4 섹션 그리드: 기본 설정 / 알림 설정 / 데이터 설정 / 테마 설정 + 연결 계정 + 시스템 정보 | **"준비 중입니다" 스텁** | **완전 부재** |

**변경 방안 I** — Settings 는 **심사 인지도가 낮다**. 마지막 우선순위.
- 최소: 테마 토글(이미 헤더에 있음), 알림 on/off 체크박스 3개만, 나머지는 "개발 중" 배지로 남겨도 심사 감점 없음.

---

## 우선순위 매트릭스 (심사 임팩트 × 구현 비용)

| 순위 | 항목 | 이유 | 예상 비용 |
|---|---|---|---|
| 🔴 **P0** | 변경 B: 대시보드 좌-우 배치 스왑 + KPI truncate + 시장 주도주 카드 | 첫 화면 1:1 매치가 가장 중요 | 소(1h) |
| 🔴 **P0** | 변경 H: /upload 페이지 구현 | 심사 기준 #2 "임의 CSV → 5초 내 자동 대시보드" = 핵심 서사 | 대(1일) |
| 🔴 **P0** | 변경 A: 사이드바 IA 재편 | 메뉴 이동 시마다 보이는 누수 | 소(30m) |
| 🟡 P1 | 변경 D: 워치리스트 풀 구현 | MSW 시드만 있으면 쉽게 완성 | 중(3h) |
| 🟡 P1 | 변경 G: /copilot 풀페이지 | 기존 로직 재활용 | 중(2h) |
| 🟡 P1 | 변경 C: 포트폴리오 히트맵/캘린더 | 시각적 인상 큼 | 중(4h) |
| 🟢 P2 | 변경 E: 심볼 지표/RSI/MACD | 기존 차트 확장 | 중(3h) |
| 🟢 P2 | 변경 F-1: 시장분석 KPI+뉴스 축소판 | 사이드바 메뉴 클릭 시 스텁 노출 방지 | 중(3h) |
| 🔵 P3 | 변경 I: /settings 최소 구성 | 심사 인지도 낮음 | 소(1h) |

---

## 권장 다음 스프린트

**Sprint-08 (심사 전 마지막)**
1. P0 3건을 analyzer-designer/frontend-engineer/backend-engineer 병렬 팀으로 Agent Teams 실행
2. P1 3건은 순차 혹은 골든패스 유지 기준으로 선택
3. P2/P3 는 "준비중" 배지 유지하되 메뉴 진입 시 한 줄 설명문 추가

심사에서 보이는 **첫 화면 3개(대시보드/포트폴리오/워치리스트)와 업로드**만 목업 수준으로 맞추면 **나머지는 "개발 중" 배지로 충분**하다. 워치리스트까지 다듬으면 심사 리스크 최소화.

---

## 캡처 파일 레퍼런스

| 페이지 | 목업 | 현재 캡처 |
|---|---|---|
| 대시보드 | `KakaoTalk_20260423_185428043.png` | `.playwright-mcp/current-00-dashboard-dark.png` |
| 포트폴리오 | `_01.png` | `current-01-portfolio.png` |
| 워치리스트 | `_02.png` | `current-02-watchlist.png` |
| 종목분석 | `_03.png` | `current-03-symbol.png` |
| 시장분석 | `_04.png` | `current-04-analyze.png` (스텁) |
| 코파일럿 | `_05.png` | (N/A — 라우트 404) |
| 업로드 | `_06.png` | `current-06-upload.png` (404) |
| 설정 | `_07.png` | `current-07-settings.png` (스텁) |
